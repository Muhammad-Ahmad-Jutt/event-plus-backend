services:
  web:
    build: .
    container_name: flask_app
    ports:
      - "5000:5000"
    env_file:
      - .env
    volumes:
      - ./logs:/app/logs
    restart: always
    networks:
      - event-plus-network
networks:
  event-plus-network:
    external: true
